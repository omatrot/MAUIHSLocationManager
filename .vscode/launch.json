{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug iOS",
      "type": "coreclr",
      "request": "launch",
      "preLaunchTask": "build", // Matches the "label" in tasks.json
      "console": "internalConsole"
    }
  ]
}